---
name: Create Workspace
description: Create a new office for an agent to work in
---
To create a workspace for a team member, create a folder named after the team member in `.agents/office/`. Workspaces are always created in `.agents/office/<workspace-name>/`. Each workspace is isolated so team members can focus on their work without interfering with other projects.