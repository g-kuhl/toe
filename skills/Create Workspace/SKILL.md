---
name: Create Workspace
description: Create a new office for an agent to work in
---
To create a workspace for a Team Member, create a folder named after the team member in the `./agents/office/` directory. Workspaces are always created in `./agents/office/<workspace-name>` Each workspace is isolated to ensure that Team Members can focus on their work without interference from other projects.